<?php

namespace {{ namespace }}\Model\Logic;

use {{ namespace }}\Model\Entity\Api\App;

class AppLogic {
	public function getByAppid($appid) {
		if (empty($appid)) {
			throw new \RuntimeException('Invalid appid');
		}
		return [
			'data' => $appid
		];
//		return App::where('appid', '=', $appid)->first();
	}
}